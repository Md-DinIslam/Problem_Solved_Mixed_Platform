#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int x, y, z;
        cin >> x >> y >> z;
        int sum = 0;
        sum = (x * 4) + (y * 2) + (z * 0);
        cout<<sum<<endl;
    }
}